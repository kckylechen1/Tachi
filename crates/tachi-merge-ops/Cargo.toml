[package]
name = "tachi-merge-ops"
version = "1.9.2"
edition = "2021"
license = "AGPL-3.0-only"
description = "Local worktree cleanup bridge used by GitHub safe-merge flows."

[dependencies]
tokio = { version = "1", features = ["process"] }
serde = { workspace = true }
serde_json = { workspace = true }
