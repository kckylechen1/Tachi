{
  "why": "Slice-2 flipped the dialect's streaming bit, and a capability claim is only real if it reaches the wire. `stream` appears exactly once, spelled `true`, and nothing else about the body moves — in particular no `stream_options`, which several OpenAI-compatible servers reject and which is therefore a wire change with its own review rather than a side effect of landing a decoder.",
  "request": {
    "target": {"kind": "resolved", "target": {"deployment_id": "dep-openai-compat-1", "endpoint": "https://provider.test/v1/chat/completions", "provider_model_id": "test-model-1"}},
    "messages": [{"role": "user", "content": {"kind": "text", "text": "hello"}}],
    "stream": "enabled",
    "data_policy": {"prohibit_training_on_input": true, "prohibit_retention": true},
    "admitted": {"caller_ref": "uid:501"}
  },
  "expected": {
    "method": "POST",
    "url": "https://provider.test/v1/chat/completions",
    "headers": [["content-type", "application/json"]],
    "auth_placement": {"kind": "header", "name": "authorization", "prefix": "Bearer "},
    "body_bytes": "{\"model\":\"test-model-1\",\"messages\":[{\"role\":\"user\",\"content\":\"hello\"}],\"stream\":true}",
    "body": {
      "model": "test-model-1",
      "messages": [{"role": "user", "content": "hello"}],
      "stream": true
    }
  }
}
