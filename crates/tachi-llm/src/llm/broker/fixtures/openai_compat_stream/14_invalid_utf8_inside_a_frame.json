{
  "grammar": "openai_compat_sse",
  "why": "A lone 0xff where a character should be. Validation is per line, not per chunk, so a multi-byte character legitimately split across two network reads still decodes — but a byte sequence that is not text at any chunking is refused.",
  "chunks": [
    {
      "text": "data: {\"choices\":[{\"index\":0,\"delta\":{\"content\":\""
    },
    {
      "hex": "ff"
    },
    {
      "text": "\"}}]}\n\n"
    }
  ],
  "terminal_input": {
    "kind": "finish",
    "eof": "clean"
  },
  "expected": {
    "events": [],
    "error": "invalid_utf8",
    "terminal_disposition": {
      "disposition": "protocol_error",
      "violation": {
        "kind": "stream_decode",
        "rule": "invalid_utf8"
      }
    }
  }
}
