{
  "why": "A 2xx never reaches classify_error through parse_response; when it is called directly it means the body was unusable. The shipped lane's `_` arm calls that LaneOutage, so the projection must agree — but the broker keeps `protocol` as its own member because our request was fine and the provider's answer was not.",
  "status": 200,
  "headers": [],
  "body": "{\"choices\":[]}",
  "expected": {
    "class": "protocol",
    "advice": "try_fallback_deployment",
    "legacy_failure_class": "lane_outage",
    "legacy_retry_intent": null,
    "retry_after": null
  }
}
