{
  "why": "The `contains(\"balance\") && contains(\"insufficient\")` arm, distinct from the `insufficient balance` phrase arm. The two words appear far apart and in the wrong order.",
  "status": 403,
  "headers": [],
  "body": "{\"error\":{\"message\":\"Your account balance is below the minimum; funds are insufficient to serve this request\"}}",
  "expected": {
    "class": "billing_or_quota",
    "advice": "retry_other_credential",
    "legacy_failure_class": "provider_exhausted",
    "legacy_retry_intent": "retry_within_tier_if_another_key",
    "retry_after": null
  }
}
