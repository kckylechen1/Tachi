{
  "why": "The shipped lane uses this value as its sleep before the next attempt, in preference to its own backoff. Dropping it would turn a 5-second wait into an exponential one.",
  "status": 503,
  "headers": [["Retry-After", "5"]],
  "body": "{\"error\":{\"message\":\"Service temporarily overloaded\"}}",
  "expected": {
    "class": "server_error",
    "advice": "retry_same_deployment",
    "legacy_failure_class": "transient",
    "legacy_retry_intent": "retry_within_tier",
    "retry_after": {"kind": "seconds", "value": 5}
  }
}
