{
  "why": "The ordering trap: the billing-marker test lives inside the 403 arm. A 401 whose body mentions billing is still an auth failure on both sides. An implementation that checked the body before the status would pass a status-only corpus and fail here.",
  "status": 401,
  "headers": [],
  "body": "{\"error\":{\"message\":\"Your billing account has an insufficient balance and your key is invalid\"}}",
  "expected": {
    "class": "auth_invalid",
    "advice": "retry_other_credential",
    "legacy_failure_class": "auth_failed",
    "legacy_retry_intent": "retry_within_tier_if_another_key",
    "retry_after": null
  }
}
