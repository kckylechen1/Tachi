[package]
name = "memory-server-hub-cli"
version = "1.9.2"
edition = "2021"
license = "AGPL-3.0-only"
description = "Hub registry CLI commands (list/show/packs/bindings/doctor/stats). Extracted from tachi-server (#833)."

[dependencies]
rusqlite = { workspace = true }
tachi-bootstrap = { path = "../tachi-bootstrap" }
dirs = "5.0"
serde_json = { workspace = true }
memcore = { path = "../memcore" }

[dev-dependencies]
chrono = { workspace = true }
tempfile = "3"
