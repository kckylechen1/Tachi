//! Test-only package for the 24 bootstrap topology tests extracted from
//! `tachi-server` (#1714 Track T). The library is intentionally empty; all
//! dependencies are dev-dependencies and all behavior lives in the integration
//! test binary.
