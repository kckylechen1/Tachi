[package]
name = "tachi-credential-profile"
version = "1.9.2"
edition = "2021"
license = "AGPL-3.0-only"
publish = false
description = "Credential-profile discovery, planning, materialization, cleanup and doctor. Carved out of tachi-server (#1610 Track T / #1611): zero MemoryServer coupling, so it lifts whole."

[dependencies]
memcore = { path = "../memcore" }
tachi-params = { path = "../tachi-params" }
chrono = { workspace = true }
dirs = "5.0"
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
libc = "0.2"
tempfile = "3"
