//! Test-only package for the durable delivery seam tests extracted from
//! `tachi-server` (#1831 Track T). The library is intentionally empty; all
//! dependencies are dev-dependencies and all behavior lives in the integration
//! test binary.
