[package]
name = "memory-server-runtime"
version = "2.0.0"
edition = "2021"
license = "AGPL-3.0-only"

[features]
test-support = []

[dependencies]
memcore = { path = "../memcore" }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
