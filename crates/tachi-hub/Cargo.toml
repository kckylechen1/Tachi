[package]
name = "tachi-hub"
version = "2.0.0"
edition = "2021"
license = "AGPL-3.0-only"
publish = false

[dependencies]
memcore = { path = "../memcore" }
chrono = { workspace = true }
rmcp = { version = "3.3.0", features = ["schemars"] }
serde_json = { workspace = true }
