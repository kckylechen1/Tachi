[package]
name = "memory-server-manifest-audit"
version = "1.6.4"
edition = "2021"
license = "AGPL-3.0-only"
description = "Project-DB relocation audit (dry-run planning). Extracted from memory-server (#833)."

[dependencies]
serde = { workspace = true }

[dev-dependencies]
tempfile = "3"
