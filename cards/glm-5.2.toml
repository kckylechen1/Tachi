schema = "tachi-lane-card/v1"

[identity]
name = "glm-5.2"
vendor = "zhipu"
model_id = "zhipuai-coding-plan/glm-5.2"
transport = "opencode"
roles = ["implementer"]

[hexagon] # seed 2026-07-05
spec_fidelity = 0.55
self_report_trust = 0.15
test_discipline = 0.25
equivalence_refactor = 0.92
security_competence = 0.10
speed_cost = 0.85

[routing]
route_to = ["equivalence-refactor", "mechanical-batch", "perf-optimization", "doc-sync"]
route_away = ["security-critical", "auth-acl", "injection-containment", "anything-self-verified"]
forbidden_without_gates = ["security"] # only under dual-track + cross-vendor adversarial review

[[failure_modes]]
signature = "fake_security_fix"
count = 4
evidence = ["tachi#541", "tachi#583", "tachi#600", "tachi#607"]
counter_clause = "Security fix: the issue body's frozen design is the ONLY solution; no alternative approach. Discriminating test mandatory (red pre-fix)."
efficacy = "prompt-layer INEFFECTIVE (adjudication comment sat on the issue; violated anyway) — only structural gates (adversarial review + leader independent verify) catch it. Do NOT return security rework to glm; surgical rework goes to codex per adjudication."

[[failure_modes]]
signature = "self_close_overreach"
count = 3
evidence = ["tachi#594", "tachi#595", "tachi#600"]
counter_clause = "Never `Closes` a partially-addressed issue; use `Refs`. Enumerate every acceptance criterion done/not-done."
efficacy = "packet-clause plausibly effective; leader verifies PR body regardless."

[[failure_modes]]
signature = "falsified_ci_report"
count = 1
evidence = ["tachi#610"]
counter_clause = "Do NOT self-report CI status. Paste verbatim gate output; leader independently re-runs clippy/audit/tests."
efficacy = "structural only: leader re-runs every gate. Sets trust.self_report=low globally."

[constraints.mandatory_clauses]
all = ["paste verbatim `test result:` lines or delivery is incomplete"]
security = ["frozen design is the only solution", "discriminating tests red pre-fix", "never Closes a security issue"]

[trust]
self_report = "low" # falsified clippy-clean checkbox (#610); every checkbox independently verified

[[eval_rows]]
date = 2026-07-05
task = "tachi#606 migrate_enum split"
score = 0.95
verdict = "dual-review MERGE-SAFE; byte-identical SQL"

[[eval_rows]]
date = 2026-07-05
task = "tachi#595 batch2 equivalence items (#543/#544/#545/#546)"
score = 0.90
verdict = "4/5 equivalence refactors clean, zero smuggling"

[[eval_rows]]
date = 2026-07-05
task = "tachi#541/#583/#600/#607 security items"
score = 0.15
verdict = "4/4 fake fixes; caller-controlled trust source, no fail-closed, zero tests"

[[eval_rows]]
date = 2026-07-05
task = "tachi#610 i18n/npm batch"
score = 0.10
verdict = "BROKEN + falsified clippy-clean self-report"
